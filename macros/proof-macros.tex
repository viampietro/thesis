%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% BEHAVIOR PRESERVATION PROOF MACROS %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\def\InCsCompT{$\mathtt{comp}(id_t,$ $"transition",$ $gm_t,$ $ipm_t,$ $opm_t)\in{}d.cs$}

\def\exT{\noindent{}By definition of $id_t$, there exist
  $gm_t,ipm_t,opm_t$ s.t. \InCsCompT.}

\def\InCsCompP{$\mathtt{comp}(id_p,$ $"place",$ $gm_p,$ $ipm_p,$ $opm_p)\in{}d.cs$}

\def\exP{\noindent{}By definition of $id_p$, there exist
  $gm_p,ipm_p,opm_p$ s.t. \InCsCompP.}

\def\upSim{$\gamma,E_c,\tau\vdash{}s\stackrel{\uparrow}{\sim}\sigma$}
\def\dwSim{$\gamma\vdash{}s\stackrel{\downarrow}{\sim}\sigma$}

\def\upSitpn{$E_c,\tau\vdash{}s\xrightarrow{\downarrow}s'$}
\def\dwSitpn{$E_c,\tau\vdash{}s\xrightarrow{\downarrow}s'$}

\newenvironment{niproof}{%
  \begin{proof}
    \setlength{\parindent}{0pt}
  }{%
  \end{proof}}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../main"
%%% End:
